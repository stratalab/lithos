name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      # CPU torch keeps CI fast; unit/shape tests don't need a GPU (PRD §26.1).
      # data + eval extras cover the extractor / export / benchmark tests + mypy;
      # pdf (docling) is intentionally excluded to keep installs quick.
      - name: Sync dependencies
        run: uv sync --extra data --extra eval
        env:
          UV_TORCH_BACKEND: cpu

      - name: Ruff
        run: uv run ruff check .

      - name: Mypy
        run: uv run mypy lithos

      - name: Pytest
        run: uv run pytest
