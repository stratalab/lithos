# Eval tasks

Custom internal eval task definitions live here. External benchmark tasks run
through the EleutherAI lm-evaluation-harness against the HF/Qwen3 export — see
`configs/eval/lm-eval.yaml` for the command path (PRD §11.2, §26.8).
